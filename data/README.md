# 核心数据与报告目录 (Data & Reports)

本工作目录遵循标准科学制图框架的指导规范，用于严格隔离数据源输入、处理中间态与最终文本输出。

- **`/raw`**：存储包含元数据 (Meta Data) 与未经处理的原口径探测表单。**该目录中的所有数据应被严控为只读**，防止篡改。
- **`/processed`**：存放经过清洗、插值及重构的中间过程数据 (Processed Data)。它是各类 Python 绘图脚本直接进行特征读取的数据集集散地。
- **`各类 *.md 报告`**：专门用于向用户提供数据洞察、逻辑反馈和最终绘制图例结论的说明性文件存放层级。
